import { IAddComponentProps } from '@/types/global/global.types';

export default function AddSetup({ setOpen, refetch }: IAddComponentProps) {
    console.log("setOpen", setOpen, refetch);

    return (
        <div>

        </div>
    )
}
